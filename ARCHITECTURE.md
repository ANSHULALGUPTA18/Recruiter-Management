# SSO Architecture Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Microsoft Azure AD                           │
│                        (Identity Provider)                           │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │  Tenant: your-tenant-id                                   │      │
│  │  App Registration: Recruiter Suite                        │      │
│  │  - Client ID                                              │      │
│  │  - Client Secret                                          │      │
│  │  - Redirect URIs                                          │      │
│  │  - API Permissions (User.Read, openid, profile, email)  │      │
│  └──────────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │   OAuth 2.0 / OIDC        │
                    │   Authentication Flow      │
                    └─────────────┬─────────────┘
                                  │
        ┌─────────────────────────┼─────────────────────────┐
        │                         │                         │
        ▼                         ▼                         ▼
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   Resume     │         │  Recruiter   │         │   Legal      │
│  Formatter   │         │    Suite     │         │ Compliance   │
│   (Child)    │         │   (Parent)   │         │   (Child)    │
└──────────────┘         └──────────────┘         └──────────────┘
        │                         │                         │
        │                         │                         │
        └─────────────────────────┴─────────────────────────┘
                    Shared Azure AD Session
```

## 📱 User Authentication Flow

```
1. User Opens Browser
   │
   ▼
2. Navigate to Recruiter Suite (http://localhost:5173)
   │
   ▼
3. ProtectedRoute Component Checks Authentication
   │
   ├─→ Not Authenticated
   │   │
   │   ▼
   │   Display "Sign in with Microsoft" Button
   │   │
   │   ▼
   │   User Clicks Sign In
   │   │
   │   ▼
   │   Redirect to login.microsoftonline.com
   │   │
   │   ▼
   │   User Enters Microsoft Credentials
   │   │
   │   ▼
   │   Azure AD Validates Credentials
   │   │
   │   ▼
   │   Azure AD Issues Tokens:
   │   - Access Token
   │   - ID Token
   │   - Refresh Token
   │   │
   │   ▼
   │   Redirect to Recruiter Suite with tokens
   │   │
   │   ▼
   │   MSAL stores tokens in sessionStorage
   │   │
   │   └─→ Authentication Complete!
   │
   └─→ Already Authenticated
       │
       ▼
       Load Dashboard
```

## 🔄 SSO Flow to Child Applications

```
User in Recruiter Suite (Authenticated)
   │
   ▼
Clicks "Resume Formatter" Link
   │
   ▼
useSSONavigation Hook:
   │
   ├─→ Get fresh access token from MSAL
   │   │
   │   ▼
   │   acquireTokenSilent()
   │   │
   │   ├─→ Token Valid → Return token
   │   │
   │   └─→ Token Expired → acquireTokenPopup()
   │
   ▼
Store token in sessionStorage (for child app)
   │
   ▼
Open child app in new window
   │
   ▼
Child App Loads
   │
   ▼
Child App Checks Azure AD Session
   │
   ├─→ Session Exists (Same Tenant)
   │   │
   │   ▼
   │   Silent Authentication (prompt=none)
   │   │
   │   ▼
   │   Azure AD Issues Token Silently
   │   │
   │   └─→ NO LOGIN PROMPT! ✅
   │
   └─→ No Session
       │
       └─→ Would show login (but shouldn't happen with SSO)
```

## 🔐 Backend Token Validation Flow

```
Frontend API Request
   │
   ▼
Get access token from MSAL
   │
   ▼
Add Authorization: Bearer <token>
   │
   ▼
Send request to Backend API
   │
   ▼
Backend authMiddleware.ts:
   │
   ├─→ 1. Extract token from header
   │
   ├─→ 2. Get public key from Azure AD JWKS endpoint
   │
   ├─→ 3. Verify token signature
   │
   ├─→ 4. Check token expiration
   │
   ├─→ 5. Validate issuer (Azure AD tenant)
   │
   ├─→ 6. Validate audience (Client ID)
   │
   ▼
Token Valid?
   │
   ├─→ YES → Attach user to request
   │   │
   │   ▼
   │   Process API request
   │   │
   │   ▼
   │   Return response
   │
   └─→ NO → Return 401 Unauthorized
       │
       ▼
       Frontend refreshes token
       │
       └─→ Retry request
```

## 🗂️ Component Hierarchy

```
App.tsx (Root)
├── MsalProvider (MSAL Configuration)
│   └── AuthProvider (Auth Context)
│       └── Router
│           └── ProtectedRoute
│               └── MainLayout
│                   ├── Navbar
│                   │   ├── Logo
│                   │   ├── Navigation Items
│                   │   │   └── SSO Navigation Handler
│                   │   └── UserDropdown
│                   │       ├── User Info
│                   │       └── Logout Button
│                   │
│                   ├── Sidebar
│                   │
│                   └── Pages
│                       └── Dashboard
│                           ├── JobCards
│                           ├── QuickLinks
│                           └── AddQuickLinkModal (Right Panel)
```

## 📦 Data Flow

```
┌──────────────────────────────────────────────────────────────┐
│                        Frontend                               │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │  AuthContext                                       │     │
│  │  - isAuthenticated: boolean                        │     │
│  │  - user: AccountInfo                               │     │
│  │  - login(): Promise<void>                          │     │
│  │  - logout(): Promise<void>                         │     │
│  │  - getAccessToken(): Promise<string>               │     │
│  └────────────────────────────────────────────────────┘     │
│                          │                                    │
│                          ▼                                    │
│  ┌────────────────────────────────────────────────────┐     │
│  │  MSAL Instance                                     │     │
│  │  - Manages authentication state                    │     │
│  │  - Handles token acquisition                       │     │
│  │  - Stores tokens in sessionStorage                 │     │
│  └────────────────────────────────────────────────────┘     │
│                          │                                    │
│                          ▼                                    │
│  ┌────────────────────────────────────────────────────┐     │
│  │  API Service (axios)                               │     │
│  │  - Adds Authorization header                       │     │
│  │  - Makes requests to backend                       │     │
│  └────────────────────────────────────────────────────┘     │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            │ HTTP Request
                            │ Authorization: Bearer <token>
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                        Backend                                │
│                                                               │
│  ┌────────────────────────────────────────────────────┐     │
│  │  authMiddleware                                    │     │
│  │  - Validates JWT token                             │     │
│  │  - Verifies with Azure AD public keys             │     │
│  │  - Attaches user to request                        │     │
│  └────────────────────────────────────────────────────┘     │
│                          │                                    │
│                          ▼                                    │
│  ┌────────────────────────────────────────────────────┐     │
│  │  API Routes                                        │     │
│  │  - /api/auth/validate                              │     │
│  │  - /api/auth/me                                    │     │
│  │  - /api/jobs/summary                               │     │
│  │  - /api/quick-links                                │     │
│  └────────────────────────────────────────────────────┘     │
│                          │                                    │
│                          ▼                                    │
│  ┌────────────────────────────────────────────────────┐     │
│  │  Business Logic (Services/Controllers)             │     │
│  └────────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────────┘
```

## 🔑 Token Lifecycle

```
Token Acquisition:
┌─────────────────────────────────────────────────────────┐
│ User logs in                                             │
│   ↓                                                      │
│ Azure AD issues tokens:                                  │
│   • Access Token (1 hour validity)                       │
│   • ID Token (1 hour validity)                           │
│   • Refresh Token (90 days validity)                     │
│   ↓                                                      │
│ Tokens stored in sessionStorage                          │
└─────────────────────────────────────────────────────────┘

Token Usage:
┌─────────────────────────────────────────────────────────┐
│ API call needed                                          │
│   ↓                                                      │
│ getAccessToken() called                                  │
│   ↓                                                      │
│ Check if token valid                                     │
│   ├─→ Valid → Use token                                 │
│   └─→ Expired → acquireTokenSilent()                    │
│       ├─→ Success → New token                           │
│       └─→ Fail → acquireTokenPopup() (re-login)         │
└─────────────────────────────────────────────────────────┘

Token Cleanup:
┌─────────────────────────────────────────────────────────┐
│ User logs out OR Closes tab                             │
│   ↓                                                      │
│ sessionStorage cleared                                   │
│   ↓                                                      │
│ Tokens removed                                           │
│   ↓                                                      │
│ User must re-authenticate                                │
└─────────────────────────────────────────────────────────┘
```

## 🌐 Multi-Application SSO

```
Same Azure AD Tenant
         │
         ├─→ Recruiter Suite (Parent)
         │   └─→ User logs in here FIRST
         │
         ├─→ Resume Formatter (Child)
         │   └─→ Reuses session, no login
         │
         ├─→ Legal Compliance (Child)
         │   └─→ Reuses session, no login
         │
         └─→ Referral System (Child)
             └─→ Reuses session, no login

All apps share:
✓ Same Tenant ID
✓ Same authentication cookies (login.microsoftonline.com)
✓ Can use same Client ID OR separate Client IDs (same tenant)
```

---

This architecture ensures:
- ✅ Single login experience
- ✅ Secure token management
- ✅ Seamless navigation between apps
- ✅ Enterprise-grade authentication
